{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
<div class="w80 m-auto">
    <h1 class="titre my-5">{{ title | upper}}</h1>

    <table class="table w100">
        <tbody>
            <tr>
                <th>IDENTIFIANT</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>PASSWORD</th>
                <td>******</td>
            </tr>
            <tr>
                <th>ROLES</th>
                <td>{{ user.roles | join(', ') }}</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex justify-content-between">
        <a class="btn btn-md btn-primary" href="{{ path('app_user') }}">Retour à la liste</a>
    </div>
</div>
{% endblock %}
