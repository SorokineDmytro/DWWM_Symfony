{% extends 'base.html.twig' %}
{% block body %}
    <div class="w80">
        <h1 class="titre">{{ title | upper }}</h1>
        {{ form_start(form) }}
            {{ form_row(form.username) }}
            {{ form_row(form.plainPassword) }}
            {{ form_row(form.roles) }}
            <div class="line-button">
                <a href="{{path('app_user')}}" class="btn btn-md btn-success">Retour à la liste</a>
                <button type="reset" class="btn btn-md btn-primary">Annuler</button>
                <button type="submit" class="btn btn-md btn-primary">Enregistrer</button>
            </div>
        {{ form_end(form) }}
    </div>
{% endblock %} 

