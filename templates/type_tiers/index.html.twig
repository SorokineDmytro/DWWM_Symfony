{% extends 'base.html.twig' %}

{% block title %}TypeTiers index{% endblock %}

{% block body %}
    <h1>TypeTiers index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Prefixe</th>
                <th>NumeroInitial</th>
                <th>Libelle</th>
                <th>Format</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_tier in type_tiers %}
            <tr>
                <td>{{ type_tier.id }}</td>
                <td>{{ type_tier.prefixe }}</td>
                <td>{{ type_tier.numeroInitial }}</td>
                <td>{{ type_tier.libelle }}</td>
                <td>{{ type_tier.format }}</td>
                <td>
                    <a href="{{ path('app_type_tiers_show', {'id': type_tier.id}) }}">show</a>
                    <a href="{{ path('app_type_tiers_edit', {'id': type_tier.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_tiers_new') }}">Create new</a>
{% endblock %}
