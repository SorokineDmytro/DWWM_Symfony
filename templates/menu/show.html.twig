{% extends 'base.html.twig' %}

{% block title %} {{ title }} {% endblock %}

{% block body %}
<div class="w80 m-auto">
    <h1 class="titre my-5">{{ title | upper }}</h1>

    <table class="table w100">
        <tbody>
            <tr>
                <th>RANG</th>
                <td>{{ menu.rang }}</td>
            </tr>
            <tr>
                <th>LIBELLE</th>
                <td>{{ menu.libelle }}</td>
            </tr>
            <tr>
                <th>URL</th>
                <td>{{ menu.url }}</td>
            </tr>
            <tr>
                <th>ROLE</th>
                <td>{{ menu.role }}</td>
            </tr>
            <tr>
                <th> ICONE</th>
                <td>{{ menu.icone }}</td>
            </tr>
        </tbody>
    </table>
    
    <div class="d-flex justify-content-between">
        <a class="btn btn-md btn-primary" href="{{ path('app_menu') }}">Retour à la liste</a>    
    </div>
</div>
{% endblock %}
